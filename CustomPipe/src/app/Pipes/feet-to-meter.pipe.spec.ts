import { FeetToMeterPipe } from './feet-to-meter.pipe';

describe('FeetToMeterPipe', () => {
  it('create an instance', () => {
    const pipe = new FeetToMeterPipe();
    expect(pipe).toBeTruthy();
  });
});
